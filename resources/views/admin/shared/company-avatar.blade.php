@if($thumbnailUrl)
    <div style="margin-bottom:10px;">
        <img src="{{ e($thumbnailUrl) }}" alt="Company avatar" style="max-width:120px; max-height:120px; border-radius:4px;">
    </div>
@endif

